---
layout: layouts/base.njk
title: Categories
description: Browse blog posts by category.
permalink: /blog/categories/
---

<section class="blog-list">
  <div class="container">
    <div class="section-title">
      <h1>Categories</h1>
      <p>Browse blog posts by category.</p>
    </div>

    <div class="category-grid">
      {% for category in collections.categoryList %}
      <a href="/blog/categories/{{ category | slugify }}/" class="category-card">
        <h3>{{ category | capitalize }}</h3>
        <p>{{ collections.postsByCategory[category].length }} post{% if collections.postsByCategory[category].length != 1 %}s{% endif %}</p>
      </a>
      {% endfor %}
    </div>

    {% if collections.categoryList.length == 0 %}
    <div class="text-center" style="padding: var(--space-16) 0;">
      <p>No categories yet. Check back soon!</p>
    </div>
    {% endif %}
  </div>
</section>
