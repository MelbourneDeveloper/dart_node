name: dart_logging
description: A logging framework with structured logging, child loggers, and console output.
version: 1.0.0

environment:
  sdk: ^3.7.2

dev_dependencies:
  austerity: ^1.3.0
  lints: ^6.0.0
  test: ^1.28.0
